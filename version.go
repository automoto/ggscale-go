package ggscale

// Version is the SDK's semantic version. It is sent as part of the
// User-Agent header on every request (see StdNetTransport) and is kept
// in sync with the module's Git tag.
const Version = "0.4.0"
